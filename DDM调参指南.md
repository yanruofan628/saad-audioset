# DDM模型调参指南

## 问题诊断

如果DDM拟合结果不理想（所有系数为0，预测准确率低），可能的原因：

1. **初始化不当**：参数初始值离最优解太远
2. **优化器设置**：迭代次数不足或容差设置不当
3. **参数边界**：边界设置不合理，限制了参数搜索空间
4. **数据问题**：特征与选择/RT的关系太弱

## 主要可调参数

### 1. `scale_factor`（缩放因子）

**位置**：`fit_ddm_mle()` 函数中，约第494行

**作用**：将逻辑回归系数转换为DDM漂移率系数时的缩放因子

**调参建议**：
- **默认值**：2.0
- **如果漂移率太小**（所有v接近0）：
  - 增大到 3.0-5.0
- **如果数值不稳定**（出现NaN或inf）：
  - 减小到 1.0-1.5
- **如果选择预测准确率低**：
  - 尝试 1.5, 2.0, 2.5, 3.0，选择准确率最高的

**示例**：
```python
scale_factor = 2.5  # 从2.0改为2.5
```

### 2. `n_tries`（优化尝试次数）

**位置**：`fit_ddm_mle()` 函数中，约第553行

**作用**：多次尝试不同的初始值，选择最佳结果

**调参建议**：
- **默认值**：3
- **如果优化不稳定**：
  - 增大到 5-10
- **如果计算时间允许**：
  - 可以增加到 10-20

**示例**：
```python
n_tries = 5  # 从3改为5
```

### 3. `maxiter`（最大迭代次数）

**位置**：`fit_ddm_mle()` 函数中，约第578行

**作用**：优化器的最大迭代次数

**调参建议**：
- **默认值**：2000
- **如果优化未收敛**：
  - 增大到 3000-5000
- **如果计算时间太长**：
  - 可以减小到 1000

**示例**：
```python
options={
    'maxiter': 3000,  # 从2000改为3000
    ...
}
```

### 4. `ftol` 和 `gtol`（容差设置）

**位置**：`fit_ddm_mle()` 函数中，约第580-581行

**作用**：
- `ftol`：函数值变化容差（越小越严格）
- `gtol`：梯度容差（越小越严格）

**调参建议**：
- **默认值**：`ftol=1e-6`, `gtol=1e-5`
- **如果优化过早停止**：
  - 减小容差：`ftol=1e-7`, `gtol=1e-6`
- **如果优化太慢**：
  - 增大容差：`ftol=1e-5`, `gtol=1e-4`

**示例**：
```python
options={
    ...
    'ftol': 1e-7,  # 更严格
    'gtol': 1e-6,  # 更严格
}
```

### 5. 参数边界（bounds）

**位置**：`fit_ddm_mle()` 函数中，约第536-546行

**作用**：限制参数的搜索范围

**调参建议**：

#### β系数边界：
```python
beta_range = max(5.0, np.abs(initial_beta).max() * 3)
```
- 如果系数被限制在边界上（说明边界太小）：
  - 增大倍数：从 `* 3` 改为 `* 5` 或 `* 10`
- 如果系数过大（说明边界太大）：
  - 减小倍数：从 `* 3` 改为 `* 2`

#### 阈值a边界：
```python
bounds.append((0.1, 5.0))
```
- **默认**：(0.1, 5.0)
- **如果a接近边界**：
  - 扩大范围：(0.05, 10.0)

#### 起始点z边界：
```python
bounds.append((0.0, 1.0))
```
- 通常不需要调整（0到1是标准范围）

#### 非决策时间t0边界：
```python
bounds.append((0.0, np.min(y_rt_valid) * 0.9))
```
- **如果t0接近边界**：
  - 可以调整为：`np.min(y_rt_valid) * 0.8` 或 `np.mean(y_rt_valid) * 0.5`

## 调参流程

### 步骤1：检查初始值
运行代码，查看"参数初始化"部分的输出：
- β系数应该**不为0**（如果都是0，说明逻辑回归失败）
- 阈值a应该在合理范围（0.5-3.0）
- 起始点z应该反映选择比例（如果65%选右，z应该>0.5）

### 步骤2：检查优化过程
查看"步骤2: 开始优化"的输出：
- 负对数似然应该**逐渐减小**
- 如果多次尝试的结果都很差，可能需要调整`scale_factor`

### 步骤3：检查拟合结果
查看"拟合结果"部分：
- β系数应该**不为0**（至少有几个显著）
- 漂移率v的范围应该合理（通常-2到2之间）
- 如果所有系数都是0，回到步骤1调整`scale_factor`

### 步骤4：检查预测性能
查看"DDM预测"部分：
- **选择准确率**：应该>0.5（至少比随机好）
- **AUC**：应该>0.5（理想>0.6）
- **RT R²**：应该>0（理想>0.1）

### 步骤5：迭代调参
如果性能不理想：
1. 先调整`scale_factor`（1.0, 1.5, 2.0, 2.5, 3.0）
2. 如果还是不行，增加`n_tries`和`maxiter`
3. 检查参数边界是否合理
4. 如果所有方法都失败，可能需要检查数据质量

## 常见问题

### Q1: 所有系数都是0怎么办？
**A**: 
1. 检查逻辑回归是否成功（查看"逻辑回归系数"输出）
2. 增大`scale_factor`（尝试3.0或5.0）
3. 检查特征是否标准化（代码中已自动标准化）

### Q2: 优化不收敛怎么办？
**A**:
1. 增加`maxiter`（3000-5000）
2. 减小容差`ftol`和`gtol`（更严格）
3. 增加`n_tries`（尝试更多初始值）

### Q3: 选择预测准确率很低（<0.5）？
**A**:
1. 检查特征与选择的关系（可能特征本身预测能力弱）
2. 调整`scale_factor`
3. 检查起始点z是否合理（应该反映选择比例）

### Q4: RT预测R²为负？
**A**:
1. RT预测通常比选择预测更难
2. 检查RT分布是否合理（是否有异常值）
3. 尝试调整阈值a的初始化（基于RT标准差）

## 快速调参模板

```python
# 在 fit_ddm_mle() 函数中修改这些参数：

# 1. 缩放因子（最重要）
scale_factor = 2.5  # 尝试：1.0, 1.5, 2.0, 2.5, 3.0

# 2. 优化尝试次数
n_tries = 5  # 尝试：3, 5, 10

# 3. 最大迭代次数
options={
    'maxiter': 3000,  # 尝试：2000, 3000, 5000
    'ftol': 1e-6,     # 尝试：1e-5, 1e-6, 1e-7
    'gtol': 1e-5,     # 尝试：1e-4, 1e-5, 1e-6
}

# 4. β系数边界
beta_range = max(5.0, np.abs(initial_beta).max() * 5)  # 从*3改为*5
```

## 参考值

根据你的数据（288个试次，RT均值0.724秒），建议的初始参数：

- `scale_factor`: 2.0-2.5
- `n_tries`: 3-5
- `maxiter`: 2000-3000
- `beta_range`: 基于逻辑回归系数 * 3-5
- `a`: 0.5-2.0（基于RT标准差）
- `z`: 0.65左右（基于65%选右）
- `t0`: 0.1-0.2秒（RT的15-30%）

